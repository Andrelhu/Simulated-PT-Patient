# Ana Agent (Console)

Run:
```bash
python -m ana_agent --model gpt-4o-mini
```

Specs expected by default:
- data/specs/behavior.txt
- data/specs/character.txt

API key:
- set OPENAI_API_KEY env var, or put it in D:\openAI_token.txt (default) or set ANA_OPENAI_KEY_FILE.
